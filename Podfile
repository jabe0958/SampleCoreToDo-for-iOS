# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleCoreToDo for iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SampleCoreToDo for iOS

  target 'SampleCoreToDo for iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleCoreToDo for iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  # Add Linkage Library
  pod 'IDZSwiftCommonCrypto', '~> 0.9.1'
  pod 'SwiftyDropbox'

end
